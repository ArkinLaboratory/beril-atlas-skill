"""CLI subcommand modules for `beril-atlas`.

Each module in this package exposes a `run(args) -> int` function that
returns an exit code. `cli.py` dispatches to these based on subparser
selection.
"""
